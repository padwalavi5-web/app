rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Authentication is handled in the app (localStorage + branch/guide passwords).
    // These rules allow the web client to read and write Firestore collections.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
